Utility repo for updating notes resources in ArchivesSpace.

### Requirements
- Node

### Usage

- `yarn install`
- `cp env-sample.json env.json`
- Update `env.json` to match ArchivesSpace instance and credentials.
- `node app.js`
